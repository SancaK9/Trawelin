
<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Edit" Class="mr-3 mb-n1" />
            Edit Accomodation?
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField @bind-Value="@updateAccomodationCommand.Id" Label="Id" ReadOnly="true"/>
        <MudTextField @bind-Value="@updateAccomodationCommand.Name" Label="Isim" />
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Info" OnClick="EditCountry">Duzenle</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    [Parameter] public UpdateAccomodationCommand updateAccomodationCommand { get; set; } = new UpdateAccomodationCommand();

    private void Cancel()
    {
        MudDialog.Cancel();
    }

    private void EditCountry()
    {
        MudDialog.Close(DialogResult.Ok(updateAccomodationCommand));
    }
}
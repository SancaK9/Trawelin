
<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Delete" Class="mr-3 mb-n1" />
            Delete Room Type?
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField @bind-Value="@roomTypeDto.Id" Label="Id" ReadOnly="true" />
        <MudTextField @bind-Value="@roomTypeDto.Name" Label="Isim" />
      
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Error" OnClick="DeleteCountry">Sil</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    [Parameter] public RoomTypeDto roomTypeDto { get; set; } = new RoomTypeDto();

    private void Cancel()
    {
        MudDialog.Cancel();
    }

    private void DeleteCountry()
    {
        MudDialog.Close(DialogResult.Ok(roomTypeDto));
    }
}